#import sickbeard
#from sickbeard import common
#from sickbeard import exceptions
#set global $title="Test Rename"
#set global $header=$show.name

#set global $sbPath=".."

#set global $topmenu="home"#
#import os.path
#include $os.path.join($sickbeard.PROG_DIR, "data/interfaces/default/inc_top.tmpl")

<input type="hidden" id="showID" value="$show.tvdbid" />

<script type="text/javascript" src="$sbRoot/js/testRename.js"></script>

<br />
<h2>Preview of the proposed name changes</h2>
<br /><br />

<table class="sickbeardTable" cellspacing="1" border="0" cellpadding="0">
#set $curSeason = -1

#for $cur_ep_obj in $ep_obj_list:
#set $curLoc = $cur_ep_obj.location[len($cur_ep_obj.show.location)+1:]
#set $curExt = $curLoc.split('.')[-1]
    #if int($cur_ep_obj.season) != $curSeason:
  <tr><td colspan="3"><a name="season-$cur_ep_obj.season"></a></td></tr>
  <tr class="seasonheader" id="season-$cur_ep_obj.season">
    <td colspan="3">
        <h2>#if int($cur_ep_obj.season) == 0 then "Specials" else "Season "+str($cur_ep_obj.season)#</h2>
    </td>
  </tr>
  <tr id="season-$cur_ep_obj.season-cols"><th width="1%"><input type="checkbox" class="seasonCheck" id="$cur_ep_obj.season" /></th><th>Episode</th><th>Old Name</th><th>New Name</th></tr>
        #set $curSeason = int($cur_ep_obj.season)
    #end if    

  #set $epStr = str($cur_ep_obj.season) + "x" + str($cur_ep_obj.episode)
  <tr class="season-$curSeason">
    <td width="1%">
      <input type="checkbox" class="epCheck" id="<%=str(cur_ep_obj.season)+'x'+str(cur_ep_obj.episode)%>" name="<%=str(cur_ep_obj.season) +"x"+str(cur_ep_obj.episode) %>" />
    </td>
    <td align="center">$cur_ep_obj.episode</td>
    <td width="50%">$curLoc</td>
    <td width="50%">$cur_ep_obj.proper_path().$curExt</td>
    </td>
   </tr>

#end for
</table><br />
<input type="submit" value="Rename selected episodes">
#include $os.path.join($sickbeard.PROG_DIR, "data/interfaces/default/inc_bottom.tmpl")
