#import sickbeard
#import datetime
#from sickbeard import db
<div class="footer">
<b>Search</b>: <%=str(sickbeard.currentSearchScheduler.timeLeft()).split('.')[0]%> |
<b>Update</b>: <%=str(sickbeard.updateScheduler.timeLeft()).split('.')[0]%> | 
<b>Backlog</b>: $sickbeard.backlogSearchScheduler.nextRun().strftime("%A")  
<br>
#set $myDB = $db.DBConnection()
#set $numShows = len($sickbeard.showList)
#set $numGoodShows = len([x for x in $sickbeard.showList if x.paused == 0 and x.status != "Ended"])
<b>$numShows shows</b> ($numGoodShows active)
</div>
</body>
</html>
